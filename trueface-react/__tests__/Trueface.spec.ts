'use strict';

import 'jest';
import { TF } from '../src';

describe('TF', () => {
    it('Should be pass sanity', () => {
        expect(true).toBe(true);
    });
    it('Should be able to create new instance', () => {
        expect(true).toBe(true);
    });
});
